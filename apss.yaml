schema: apss.project/v1

project:
  name: syntropic137
  apss_version: v1

tool:
  hooks:
    # Keep pre-commit validation enabled unless it is temporarily blocking critical refactoring.
    pre_commit: true

standards:
  code-topology:
    id: APS-V1-0001
    version: ">=0.1.0"
  architecture-fitness:
    id: APS-V1-0002
    version: ">=0.1.0"

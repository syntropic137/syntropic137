[project]
name = "aef-domain"
version = "0.1.0"
description = "Core domain model for Agentic Engineering Framework"
requires-python = ">=3.12"

dependencies = [
    "aef-shared",
    "pydantic>=2.12.5",
]

[build-system]
requires = ["uv_build>=0.9.13"]
build-backend = "uv_build"


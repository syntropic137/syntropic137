---
title: Control
description: Pause, resume, cancel, and inject context into running executions
full: false
_openapi:
  toc:
    - depth: 2
      title: Pause Execution Endpoint
      url: '#pause-execution-endpoint'
    - depth: 2
      title: Resume Execution Endpoint
      url: '#resume-execution-endpoint'
    - depth: 2
      title: Cancel Execution Endpoint
      url: '#cancel-execution-endpoint'
    - depth: 2
      title: Inject Context Endpoint
      url: '#inject-context-endpoint'
    - depth: 2
      title: Get Execution State Endpoint
      url: '#get-execution-state-endpoint'
  structuredData:
    headings:
      - content: Pause Execution Endpoint
        id: pause-execution-endpoint
      - content: Resume Execution Endpoint
        id: resume-execution-endpoint
      - content: Cancel Execution Endpoint
        id: cancel-execution-endpoint
      - content: Inject Context Endpoint
        id: inject-context-endpoint
      - content: Get Execution State Endpoint
        id: get-execution-state-endpoint
    contents:
      - content: >-
          Pause a running execution.


          Asynchronous. This queues a pause signal and returns at once, so the

          returned `state` is the state read *before* the signal was queued and
          an

          immediate re-read may still report it. The execution reaches `paused`
          only

          once the executor picks the signal up at its next yield point.
        heading: pause-execution-endpoint
      - content: >-
          Resume a paused execution.


          Asynchronous. This queues a resume signal and returns at once, so the

          returned `state` is the state read *before* the signal was queued and
          an

          immediate re-read may still report `paused`.
        heading: resume-execution-endpoint
      - content: >-
          Cancel a running or paused execution.


          Asynchronous. This queues a cancel signal and returns at once, so the

          returned `state` is the state read *before* the signal was queued and
          an

          immediate re-read may still report `running`. That is not a refused
          cancel;

          the execution reaches `cancelled` once the executor picks the signal
          up.
        heading: cancel-execution-endpoint
      - content: >-
          Inject a message into the execution context.


          Asynchronous. This queues the message and returns at once; the agent
          sees it

          when the executor next checks for signals. The returned `state` is the
          state

          read before queuing - injection does not change it.
        heading: inject-context-endpoint
      - content: Get current execution state.
        heading: get-execution-state-endpoint
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

<APIPage document={"./openapi.json"} webhooks={[]} operations={[{"path":"/executions/{execution_id}/pause","method":"post"},{"path":"/executions/{execution_id}/resume","method":"post"},{"path":"/executions/{execution_id}/cancel","method":"post"},{"path":"/executions/{execution_id}/inject","method":"post"},{"path":"/executions/{execution_id}/state","method":"get"}]} showTitle={true} />
#!/usr/bin/env bash
# pre-commit hook
#
# Topology is no longer committed — CI regenerates it ephemerally via
# `just fitness-check`. See #293.
#
# Install: just install-hooks
set -euo pipefail

# (placeholder — add future pre-commit checks here)

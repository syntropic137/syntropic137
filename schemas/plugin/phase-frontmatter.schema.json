{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://syntropic137.dev/schemas/plugin/v0.26.0/phase-frontmatter.schema.json",
  "$defs": {
    "PhaseExecutionType": {
      "description": "How a phase should be executed.",
      "enum": [
        "sequential",
        "parallel",
        "human_in_loop"
      ],
      "title": "PhaseExecutionType",
      "type": "string"
    }
  },
  "additionalProperties": false,
  "description": "Schema for the YAML frontmatter in phase .md prompt files.\n\nThis models only the optional metadata fields that appear in phase markdown\nfrontmatter \u2014 NOT the full phase definition (which includes required fields\nlike id/name/order that come from workflow.yaml, not frontmatter).\n\nSource of truth for schemas/plugin/phase-frontmatter.schema.json.\nSee ADR-053 for the schema generation strategy.",
  "properties": {
    "model": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Model to use for this phase (e.g., 'sonnet', 'opus').",
      "title": "Model"
    },
    "allowed-tools": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      ],
      "description": "Tools AVAILABLE during this phase, restricting what the agent can reach for. Accepts a YAML list or a comma-separated string (e.g., 'Bash, Read'). Names are Claude built-ins and are validated; case is forgiven. Omit to leave the phase unrestricted. Not supported on the codex provider, which has no tool-name concept.",
      "title": "Allowed-Tools"
    },
    "max-tokens": {
      "anyOf": [
        {
          "type": "integer"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "UNSUPPORTED - declaring it is an error. No harness CLI has a token-cap flag, so this never bounded anything. Use timeout_seconds.",
      "title": "Max-Tokens"
    },
    "timeout-seconds": {
      "anyOf": [
        {
          "type": "integer"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Phase timeout in seconds.",
      "title": "Timeout-Seconds"
    },
    "execution-type": {
      "anyOf": [
        {
          "$ref": "#/$defs/PhaseExecutionType"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Phase execution type ('sequential', 'parallel', or 'human_in_loop')."
    },
    "description": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "What this phase does.",
      "title": "Description"
    },
    "argument-hint": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Hint for Claude Code command argument.",
      "title": "Argument-Hint"
    }
  },
  "title": "PhaseFrontmatterSchema",
  "type": "object"
}
